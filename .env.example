# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # Required for backend to fetch user data

# Fallback user (used when no auth token; optional for web app)
USER_ID=user-uuid-here

# OpenAI (required for copilot chat)
OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://...   # for Azure or other OpenAI-compatible API

# Server
# PORT=3001
# CORS_ORIGIN=https://your-frontend-domain.com   # restrict CORS in production
